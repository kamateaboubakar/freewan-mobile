import 'package:flutter/material.dart';

class CompteBancairePage extends StatelessWidget {
  const CompteBancairePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Mes comptes bancaires"),
      ),
    );
  }
}
