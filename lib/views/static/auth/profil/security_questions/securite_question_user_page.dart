import 'package:flutter/material.dart';

class SecuriteQuestionUserPage extends StatelessWidget {
  const SecuriteQuestionUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
