import 'package:flutter/material.dart';

class CryptoWalletPage extends StatelessWidget {
  const CryptoWalletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Mes comptes crypto"),
      ),
    );
  }
}
